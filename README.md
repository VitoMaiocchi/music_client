# Subsonic Client

Flutter implementation of the Subsonic Music Steaming API

add `credentials.env` to root dir:

```
SUBSONIC_URL=https://example.com
SUBSONIC_USER=username
SUBSONIC_PASSWORD=passwd
```